# my-code-agent 插件化总清单

> 基线：2026-08-29。本文合并原“插件化减法重构清单”和“能力组件插件化清单”，是插件化边界、现状盘点、执行路线和验收口径的唯一清单；架构分层仍以 [架构规范](./架构规范.md) 为唯一来源。

## 1. 要解决的问题

当前实现把以下四类对象放入同一个“能力组件”模型：

1. 宿主内部实现：运行时、权限、安全、存储、事件、MCP Host。
2. 可安装产品扩展：Agent 工具包、模型协议适配、可选桌面功能。
3. 外部集成实例：MCP Server、未来的 SSH 或其他连接。
4. 声明式资源：Skill、Profile、Provider、模型和 Memory 条目。

它们的生命周期、所有权、用户操作和安全边界不同。强行共用 `component/package/contribution/productClass/hostSurface/source/kind/generation` 会产生重复目录、伪组件、两跳操作和没有实际收益的替换事务。

本轮目标不是改造出更大的插件框架，而是减少概念、公开入口和运行时分支，使产品体验接近 VS Code 的有序分类，运行时边界采用 DSH 的窄接口与可逆资源释放。

## 2. 目标模型

```text
稳定宿主（普通核心代码，不在用户插件页出现）
├─ 宿主与安全：Electron、preload、权限、PathGuard、锁、事件不变量
├─ 运行时：AgentRuntime、会话、事件投影、ToolPool、MCP Host
└─ 可替换槽位：AgentEngine、ModelRouter、SessionStore（仅内部受控替换）

扩展（用户可安装/停用/卸载）
├─ Agent 工具包
├─ 模型协议适配
└─ 桌面功能扩展

集成（用户配置的外部服务实例）
└─ MCP Server、未来 SSH/数据库/远程服务连接

资源（声明式数据，不作为代码插件）
└─ Skill、Profile、Provider、模型、Memory
```

### 2.1 用户界面

用户只应看到两个管理面：

| 管理面 | 展示对象 | 直接操作 | 不展示的对象 |
|---|---|---|---|
| 扩展 | 可安装的产品功能 | 安装、启用、停用、卸载、查看权限和版本 | 运行时、权限、安全、存储、MCP Host |
| 集成 | 外部服务连接实例 | 新增、编辑、信任、连接、停用、删除、查看工具/健康 | MCP 工具的内部动态注册、连接池、信任存储实现 |

内部核心只在“诊断”显示版本、健康和故障证据，不提供伪卸载、伪替换或与扩展混排的入口。

### 2.2 运行时

- 普通核心模块保持普通 TypeScript 模块，不要求 manifest、generation 或 lifecycle manager。
- 真正可安装代码只使用一种 `ExtensionManifest` 和一种 activate/dispose 协议。
- `ExtensionManifest` 可声明 `displayName`、安全 `icon` 引用以及 Agent `agentConfig`（`timeoutMs`、`maxConcurrent`）；前两者用于开发者/用户识别扩展，后者只表示宿主可执行的默认值与上限，不授予扩展绕过权限、安全链或资源配额的能力。
- MCP Server 是 Integration 实例：配置创建 client 实例，client 贡献工具；它不是应用扩展包，也不把每个工具登记成扩展。
- Skill、Profile、Provider、Memory 都是受校验的数据资源；可启停不等于可执行插件。
- 只有 AgentEngine、ModelRouter、SessionStore 等存在多个成熟实现且影响会话一致性的槽位，保留内部受控切换。不要将此机制抽象成面向所有模块的通用组件管理器。

## 3. 设计约束

1. 一个对象只归属于“核心、扩展、集成、资源”之一，不能同时出现在两个用户目录。
2. 用户操作必须落在叶子对象上；分组标题只能提供集合操作，不能伪装成实例。
3. 没有第二个实现或第二个消费者，不新增 registry、manager、facade、lease 或 manifest 字段。
4. 可替换不等于热替换。除 UI 小功能外，默认在下个会话或重启后生效。
5. 所有工具仍必须经过 `ToolPool -> ToolPresentation -> permission/security/trace/abort`，减重不得削弱强制链。
6. 前端不从核心对象推导产品分类；产品分类必须是少量稳定的 UI 数据，而非将底层字段逐项投影。
7. 每一项迁移必须先删除旧入口，再引入新入口；短期兼容层必须写明删除条件。

## 4. 与 VS Code、DSH 的取舍

| 参考 | 采用 | 不照搬 |
|---|---|---|
| VS Code | 用户只管理扩展；工作台、扩展宿主、贡献点与具体视图分离；树形导航只组织用户任务 | 大型 extension host、全面 marketplace、任意扩展直接贡献全部 workbench API |
| DSH | 窄服务接口、明确资源 dispose、一个 MCP 配置对应一个 client 实例、配置与实现分离 | Cordis 全量运行时、所有模块都可 patch、插件树直接成为桌面产品信息架构 |
| 本项目 | 桌面端以“扩展 / 集成 / 资源设置 / 诊断”组织；后端只为真实替换点保留内部 slot | 将每个类、Pane、路由、数据条目都包装为可管理组件 |

## 5. 执行清单

### R0：冻结扩张并建立基线 [P0]

- [x] 停止新增 `CapabilityComponent` 分类、Required binding、第一方 pseudo-package 和用户目录节点。
- [x] 记录当前 `component/package/contribution` 公开 API、生产代码行数、测试数和用户入口数，作为删除基线；详见 [插件化减法基线](./插件化减法基线.md) 和自动生成 JSON。
- [x] 为现有能力逐项标注最终归属：核心、扩展、集成或资源；有争议的项目不得继续迁移。
- [x] 增加 `pluginization-freeze` 门禁：Required manifest、第一方 pseudo-package 和 Pane 注册只能下降，增加必须显式更新基线。

验收：新需求不再以“先进入组件目录”为默认方案；每项都有单一归属。R0 采用截止 2026-08-29 的可复查数据，后续减法允许指标下降。

### R1：先纠正产品信息架构 [P0]

- [x] 将当前“能力组件”用户入口改为“扩展”，只显示实际可安装的 Agent 工具包、模型协议适配和桌面功能扩展。
- [x] 建立独立“集成”入口，MCP Server 直接列为实例；添加/发现为分组动作，实例详情直接提供操作。
- [x] 将非功能的宿主底座、运行时、权限与安全、存储与事件、协议适配移出用户管理列表，改到只读诊断。
- [x] 从主区移除不再对应真实用户任务的通用“组件详情页”；扩展页与集成详情各自使用明确的操作页。

完成记录（2026-08-29）：新增 `/api/components?view=management` 宿主投影，返回 `extensions`、`integrations` 和可恢复扩展；桌面同步与管理均使用该投影，内核 catalog 仅由宿主服务和恢复流程直接读取。桌面面板改名为“扩展与集成”，按扩展（桌面端/Agent/服务端）和集成（MCP Server）分组，核心 Required 组件不再渲染；MCP 实例直接打开详情，添加动作仍在集成分组标题上完成。

真实验收记录（2026-08-29）：MCP 集成验收通过。全局与项目配置均可发现，兼容 `servers`/`mcpServers` 两种顶层格式；`image_mcp`、`duckduckgo` 可在“集成 → MCP Server”直接显示和操作。信任后目标 Server 即时重连，组件状态正确反映连接中/已启用/连接失败；启停、删除和重启后的配置、信任状态保持一致。

验收：用户不会在一个列表中同时看到 Host、Pane、工具包和 MCP Server；任一操作不超过一跳。定向组件路由与 Pane 测试通过。

### R2：拆开扩展包、集成实例和资源 [P0]

- [x] 定义最小 `ExtensionManifest`：身份、版本、贡献类型、权限、兼容性和入口；通过 `extensionManifestFromPackage` 从旧 package 声明生成产品扩展声明，扩展声明不再携带 `productClass`、`hostSurface` 等分类字段。
- [x] 定义 `IntegrationRecord`：连接配置引用、信任指纹、启用状态、健康与实例生命周期；MCP Server 通过 `mcpIntegrationRecord` 生成独立实例记录，并提供 `/api/integrations` 脱敏投影。
- [x] 将 Skill、Profile、Provider、Memory 从 package/component 术语中移除，统一归入声明式资源目录与既有服务。`/api/resources/catalog` 现在只返回元数据、状态和指纹，不返回 prompt、Skill 正文、路径或密钥。
- [x] 明确 `UIContribution` 只是扩展挂载点，不自动意味着可安装包，也不自动生成用户行；只有宿主明确登记且管理投影允许的功能扩展才进入桌面列表。

完成记录（2026-08-29）：新增 `src/agent/extension-manifest.ts`、`src/agent/integrations.ts`；资源目录补齐 Provider/Memory；MCP 增加脱敏 IntegrationRecord 投影。旧 `CapabilityComponentManifest` 和 package API 暂留为内部兼容层，R3 再收缩核心替换机制。

验收：从 manifest 可判断“扩展能贡献什么”，从 integration record 可判断“外部实例如何运行”，两者没有相互依赖的分类例外。

### R3：收缩核心替换机制 [P1]

- [x] 将 `CapabilityComponentManager` 的生产替换目录缩至 `AgentEngine`、`ModelRouter`、`SessionStore` 三个明确 slot。
- [x] 将 permission/security/MCP Host 从“面向用户的 Required Component”降为核心服务；保留服务自身健康检查与故障诊断，不提供生产替换事务。
- [x] 删除不再被真实替换使用的非核心 generation/lease/rollback 分支和测试夹具；三个核心 slot 仍保留必需的 generation、lease、rollback。可选工具包的 package recovery 属于安装生命周期，继续由恢复 CLI 管理，不与核心替换事务混用。
- [x] 为三个 slot 使用统一但内部私有的 provider contract，切换默认只在下个 session 或进程重启生效。

完成记录（2026-08-29）：生产 `REQUIRED_COMPONENT_MANIFESTS` 从 8 个收缩为 3 个；权限、安全解析、MCP Host、ToolPresentation 不再注册或绑定到替换目录。旧非核心替换分支与测试夹具已删除；核心 generation/lease/rollback 保留，读取旧 session 时丢弃已移除服务的 provider 引用。可选包恢复仍走独立安装生命周期。

验收：核心替换机制的对象数、事件类型和持久化字段显著减少；普通工具与 UI 不再触及该机制。

### R4：收敛真实扩展生命周期 [P1]

- [x] 将已经存在的第一方 Agent 工具按工具包而非单工具逐项安装，例如 Memory、Planning、Git、Web、File Editing。
- [x] 定义扩展唯一生命周期：install -> validate -> enable -> activate -> dispose -> uninstall；默认不承诺热更新。新增宿主侧 `ExtensionLifecycle` facade，复用 `CapabilityComponentManager` 的依赖、信任和状态投影；激活上下文只提供受控资源登记，dispose 按逆序清理并在失败时进入 `failed`，不会自动回滚为“已安装”。
- [x] 只为有独立代码和独立用户价值的桌面功能提供扩展资格；Search/Git 等可以保留为第一方扩展，布局和 Chat 主体不进入扩展系统。`isExtensionEligible` 对宿主保留面和外部 `server-route`/`model-adapter` fail-closed。
- [x] 扩展 API 只暴露受控的工具、设置、UI 挂载和事件句柄，不暴露 `AgentRuntime`、`ServerContext`、Electron、密钥或安全存储。新增 `createExtensionApi`，所有贡献 ID 按扩展命名空间隔离，设置名拒绝 secret/token/password/key 等敏感字段。

验收：一个扩展停用/卸载不会留下工具、UI、订阅、worker 或子进程；不会为单个按钮创建包。

完成记录（2026-08-29）：R4 第一、二刀完成。生命周期编排集中在 `src/agent/extension-lifecycle.ts`，桌面 HTTP 路由和 `components:recover` CLI 共用该 facade；`extension-manifest.ts` 固化扩展资格，`extension-api.ts` 固化受控贡献面。不加载任意入口脚本、不暴露运行时宿主对象。安装失败和激活失败均 fail-closed，已登记资源全部尝试清理，正常卸载前必须先 dispose。第一方包声明继续由既有 manifest/catalog 提供，未引入第二套安装状态。第三方仿真验收补齐 package 声明校验、未信任安装、显式信任、启用/激活、受控工具执行链、逆序释放、卸载和重启后不复活；该流程仍不加载第三方入口脚本。

### R5：删除兼容层并压缩测试 [P1]

- [x] 删除已无消费者的第一方单包安装 helper；保留实际使用的 component package catalog 和恢复生命周期。
- [x] 删除前端旧全局 `registerPane/getPane` 缓存桥，pane 直接登记宿主 `UIContributions`；保留真实 `ui.pane.*` 稳定 ID 和内部 `/api/components` 目录。
- [x] 删除无消费者的 `component` 旧目录 API、旧目录 HTTP 投影和伪 package 兼容映射；仍被 ToolPool、恢复 CLI 或内核诊断消费的稳定 catalog/package 声明保留。
- [x] 压缩兼容夹具：前端 pane 测试直接使用 `UIContributions`，删除 `registerPane/getPane` 兼容入口断言；保留扩展生命周期、MCP 信任/连接、工具安全链、live/replay 和迁移测试。
- [x] 清查并删除未被实际调用的 host contract、manifest 字段和恢复逻辑；每次删除前以搜索和覆盖测试证明无消费者。已删除项见下方矩阵，仍有消费者的核心字段明确保留。
- [x] 在 `npm run test:report` 中补充重构区域的生产代码行数、模块数、公开入口变化，防止“减重”反而加重。

完成记录（2026-08-29）：删除无消费者的 `installFirstPartyComponentPackage` 公开 helper；pane 直接登记 `UIContributions`，布局不再回退到全局 pane 缓存；MCP Server 不再伪依赖已降为核心服务的 `mcp-host-integration` 组件。前端同步已迁移至 management 投影，删除旧完整 `/api/components` 目录 GET 入口及其 `availablePackages` 兼容载荷；组件动作、恢复 CLI 和内核 catalog 仍保留真实消费者。兼容测试夹具已改为注册表契约，测试报告新增生产 footprint 指标。

### R5 消费者矩阵（2026-08-29）

| 项目 | 处理 | 证据与结论 |
| --- | --- | --- |
| `installFirstPartyComponentPackage` | 已删除 | 无生产调用者；恢复 CLI 已改走 `ExtensionLifecycle.install`，测试直接注册 manifest。 |
| 全局 `registerPane/getPane` | 已删除 | 生产 pane 全部直接登记 `UIContributions`；布局只消费 `ui.pane.*` 注册表。 |
| `replacementGroup`、`requiredContract` | 保留 | `validateCapabilityComponentManifest`、Required provider binding/lease、核心替换预检和 catalog 仍直接消费。 |
| `dependencies`、`parentId` | 保留 | 组件依赖排序、禁用树、MCP/模型协议子组件关联仍直接消费；MCP Server 已移除对核心 host 的伪 parent 依赖。 |
| `providedBy`、`source`、`productClass`、`hostSurface` | 保留 | 第一方包恢复、桌面管理投影、扩展资格检查和 MCP 集成分类仍直接消费。 |
| `generation`、`lease`、`rollbackProviders` | 保留 | 仅三个核心 Required slot 使用，且有 session 持久化、回滚 CLI 和覆盖测试；不属于兼容层。 |
| `components:recover` 与 `rollbackRequired` | 保留 | 仍是无桌面故障恢复入口和核心替换回滚路径；删除会失去服务端/CLI 自救能力。 |

因此本条的删除范围已经闭环：无消费者的兼容入口已删除；矩阵中保留项均有生产消费者和测试覆盖，不再作为“旧兼容层”处理。

完成记录（2026-08-29）：R5 兼容面已完成实际减重：旧兼容出口引用 `19 -> 0`，公开入口 `152 -> 148`，声明测试块 `2596 -> 2573`（减少 23）。R1-R4 新增的产品能力不计入减法范围；新增范围与兼容面分开记录，避免以全量 `src` 行数掩盖真实删除。

验收：R0 基线中的旧兼容面、公开入口和重复夹具净减少；不存在两套并行的扩展/组件协议。全量生产 footprint 仍单独报告，不将新增产品能力错误归因于减法。

## 6. 能力盘点与单一归属

所有对象必须只归属于“核心、扩展、集成、资源、开发基础设施”之一。这里的归属是产品生命周期归属，不等同于代码目录或运行时依赖关系。

| 类别 | 当前对象 | 用户操作 | 结论 |
|---|---|---|---|
| 核心宿主 | Electron/preload、启动退出、权限、安全 parser、PathGuard、锁、事件终态、ToolPresentation、MCP Host、布局和 Chat 主闭环 | 诊断、修复、升级 | 普通核心代码，不进入插件管理页 |
| 核心替换槽位 | AgentEngine、ModelRouter、SessionStore | 受控替换、审批、回滚 | 只保留三个 Required slot，不做通用热插拔 |
| 扩展 | Agent 工具包、桌面面板、语言服务、模型协议适配、受限渲染/附件贡献 | 安装、信任、启用、停用、卸载 | 使用统一 ExtensionManifest 与 activate/dispose |
| 集成 | MCP Server、未来 SSH/数据库/远程服务连接 | 新增、编辑、信任、连接、停用、删除 | 每个外部服务是独立实例，不是代码扩展包 |
| 资源 | Skill、Profile、Provider、模型、子 Agent 定义、Memory、主题/图标 | 查看、编辑、启停、删除 | 声明式数据，不加载任意 JS |
| 开发基础设施 | 构建、测试、coverage、replay、评测、CI/CD、发布脚本 | 开发者使用 | 不进入生产插件系统 |

### 6.1 跨 Agent 与桌面扩展

跨多个宿主面的功能仍然只有一个扩展包、一个 manifest、一个生命周期和一个权限声明，不拆成两个插件，也不在列表中重复出现。

- manifest 的 `contributions` 可以同时声明 `agent-tool` 和 `desktop-ui`。
- 管理列表选择一个主归属：以用户主要使用入口或主要产品价值为准。
- 详情页显示“Agent + 桌面端”能力标签，并按贡献类型分区展示工具、设置和 UI 操作。
- 两端价值同等重要时，显示为“跨端扩展”标签；这只是展示标签，不新增第三套生命周期或底座协议。
- 任一贡献停用/卸载都必须由同一个 dispose 事务清理，不能留下另一端的工具、面板或事件订阅。

## 7. 统一组件化推进路线

### P0：底座边界（已完成）

- manifest、来源/签名、兼容性、权限声明、资源上限和安装隔离校验。
- trust、enable/disable、health、dispose、失败终态和 CLI 恢复入口。
- AgentEngine、ModelRouter、SessionStore 三个 Required replacement slot。
- permission、security、PathGuard、trace、abort、事件终态组成不可覆盖的宿主执行链。

### P1：可选能力（已完成）

- 第一方 Agent 工具包按 manifest 注册，ToolPool/ToolPresentation 保持统一投影。
- MCP Server 作为 Integration 实例，由 MCP Host 负责连接、信任和工具生命周期。
- 模型协议适配、Search/Git/Problems/TypeScript 等受控 UI 或服务贡献完成最小接入。
- Skill、Profile、Provider、子 Agent、Memory 归入声明式资源目录。
- [x] EXT-02：受控 Agent Tool 贡献点。已激活扩展只能经宿主 `ExtensionToolRegistry` 登记为标准 `AgentTool`，再进入 `ToolPool -> ToolPresentation`；`standard` 的开放投影可见，`minimal` 仍只按显式名单可见。权限、workspace 边界、风险级别和执行链由宿主根据声明固定，贡献回调只接收输入和 abort signal，不接触 `AgentRuntime`、`ServerContext` 或 Electron。生命周期释放登记项后，后续投影立即消失；当前只由第一方/仿真宿主 hook 验证，绝不导入或执行第三方 package entry。
- [x] EXT-03：扩展 Agent 执行限额。`agentConfig.timeoutMs` 与 `maxConcurrent` 已由 `ExtensionToolRegistry` 在运行时强制：同一扩展共享并发槽位，超时和调用方取消均传递 abort signal；不响应取消的回调在实际结束前仍占用槽位，停用后的既有工具定义 fail-closed。限额不能由扩展回调自行修改。
- [x] EXT-04/05：第一方工具的受控扩展试点已验证 host 生命周期、状态恢复与安全执行链，随后按产品粒度收回。`file_outline`、`git-status` 及其余内置工具均保持 native ToolRegistry 注册，在桌面端统一归入“Agent 工具”集合；集合可整体启停，详情页可单独启停子工具。第三方受控扩展仍通过 `ExtensionToolRegistry` 登记，且不执行任意第三方 JS。
- [x] EXT-01 第三方扩展管理最小闭环：API/CLI 支持安装、查看、信任、启用、停用和卸载；完整 package manifest、来源、指纹和信任状态独立持久化；未信任扩展不能激活，卸载后重启不会复活；当前仍不加载第三方 JS。

### P2：收窄贡献面（下一阶段）

- 将 prompt section、event renderer、attachment/preview provider 做成受限贡献点。
- 收窄 AgentRuntime/ServerContext，只向扩展提供声明过的能力句柄。
- 将 route registry 改为带权限、prefix、body limit、超时和 dispose 的受控注册表。
- 明确 desktop host、window type 和 server-binding strategy 的边界。
- Creator 只能创建/验证 Profile 草稿，不能静默信任、启停、替换或发布组件。

### P3：有真实需求后再做

- 第三方可执行扩展的 worker/process 沙箱、IPC schema、资源配额和崩溃隔离。
- 父扩展的 namespaced child contributions，由父扩展维护语义，底座只治理资源和生命周期。
- 市场、升级、依赖求解、兼容矩阵、回滚历史和供应链审计。

## 8. 统一验收口径

一项能力只有同时满足以下条件才算完成组件化：

- 有稳定 manifest、唯一 ID、版本、来源、kind、capability 和依赖声明。
- 有窄宿主接口，不读取全局 runtime、ServerContext 或 Electron 对象。
- 有 trust、enable/disable、health、dispose 和失败终态。
- Optional 卸载后不残留工具、路由、事件、worker、子进程、窗口或锁。
- Agent 可见性来自 Profile 投影，权限和证据约束不能因扩展切换而消失。
- live/replay/refresh/fork 对组件 generation 和最终状态解释一致。
- 有单元、集成、失败矩阵和打包桌面验收；安全关键替换另有 shadow 对比。
- 有 CLI 恢复路径，文档和生成 catalog 与运行时注册真源一致。

## 9. 不做的事

- 不引入 Cordis，不把项目改造成运行时插件树。
- 不实现 arbitrary third-party JavaScript 的即时加载、热卸载或无限嵌套插件。
- 不把核心安全、权限、事件不变量、锁或 Electron/preload 交给普通扩展替换。
- 不把 MCP Server 的每个 tool、Skill 条目、Provider 配置或 Memory 条目做成可安装代码包。
- 不为了“统一”把扩展页、集成页和诊断页强行复用同一套列表和详情组件。

## 10. 完成标准

1. 用户能用一句话区分扩展、集成、资源和诊断。
2. 用户页面不展示底座实现；MCP Server 只在集成中出现，且直接可操作。
3. 后端只有一套真实扩展生命周期和一套独立的集成实例生命周期。
4. 高风险安全链不因减重产生旁路。
5. R0 基线相比，重构范围内生产代码、公开 API 和重复测试净减少，并在每个阶段提交中记录数据。
