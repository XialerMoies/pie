# my-code-agent 任务清单

> 更新日期：2026-08-24
> 整合来源：`backlog.md`、`agent-capability.md`、`competitor-roadmap.md`
> 说明：本文件只保留当前未完成事项和执行中的主线；已完成的成体系任务移至 [已完成任务归档.md](/E:/my-code-agent/docs/已完成任务归档.md)，不再占用本清单位置。

## 当前发布与验证任务

- [ ] 在真实发布环境执行 `npm run release:check`。
- [ ] 补充真实 Provider、真实用户数据目录、升级迁移、更多平台 shell、宿主休眠/断电恢复和长期运行矩阵；keyless replay 与本地流程测试不能替代这些环境验收。

P0 发布硬化、近期基础设施和 T-01～T-07 测试质量体系已完成，实施记录移至 [已完成任务归档.md](/E:/my-code-agent/docs/已完成任务归档.md)。当前测试事实以 [自动生成统计](/E:/my-code-agent/docs/generated/test-stats.md) 和 [Agent 行为报告](/E:/my-code-agent/docs/generated/agent-behavior-report.md) 为准。

当前工程主线：**E0-b 子 Agent 与 Provider 兼容边界收尾**；高成本原生引擎工作仍按下述触发条件启动。


## 长期 Agent 引擎路线（触发式）

### 已确认决策

- **短期继续使用 PI**：只跟进选定的稳定版本和安全修复，不追逐每个上游版本。
- **fork 保持薄**：只保留少量、可单独测试、能按行为重放的补丁；不把深度魔改继续堆入 PI fork。
- **不做深 fork**：它既承受高频上游冲突，又无法获得真正独立的架构边界，长期成本最高。
- **深度定制进入自有引擎**：E1 触发后，新的执行循环、事件协议、会话策略和编排能力进入 `NativeAgentEngine`；触发前不为预想中的定制提前建设第二套运行时。

### Phase E0：建立 AgentEngine 防腐层

**目标：** 服务端、主 Agent 和子 Agent 不再直接依赖 PI 的 `AgentSession`、`ModelRuntime` 和 `SessionManager`。

E0-a 最小引擎契约已完成并归档；本节只跟踪尚未完成的 E0-b。

**E0-b（当前）：** 将剩余 PI 接入收口到 `PiAgentEngine`，完成主会话、子 Agent、设置页和 SSE 的适配迁移。

**usage 契约边界：**

- `ProviderUsage` 表示单次 provider 请求返回的原始 input、output 和 cache 计量。
- `ContextUsageSnapshot` 表示当前上下文占用及 `exact`、`mixed`、`estimated` 来源。
- `CostLedger` 只消费 `ProviderUsage` 与 `PriceCatalog` 生成金额和预算记录，不扫描会话文件，也不重新计算 Token。
- `PiAgentEngine` 与未来的 `NativeAgentEngine` 必须提供相同契约，不能各自定义一套 usage 语义。

**E0-b 完成标准：**

- 现有 PI 接入全部收口为 `PiAgentEngine`，行为保持不变。
- PI SDK 的直接 import 只允许出现在适配器和 SDK 合约测试中，并有结构门禁。
- 主会话、子 Agent、设置页和 SSE 通过项目契约访问引擎。

## Agent Profile 与工具呈现路线（dsh 借鉴）

当前项目已有 `PermissionMode`，但它只负责授权；dsh 的 preset 和 tool presentation 还没有对应的独立抽象。新增能力必须保持三层分离：

```text
AgentProfile       → 当前 Agent 拥有哪些能力和提示词
ToolPresentation   → 模型以 native、ptc 或 both 方式调用能力
PermissionMode     → 具体调用是否允许执行
```

- [x] **AP-01 [P0][指令遵从] 引入 AgentProfile 注册表与 session 能力投影**
  - 新增 `minimal`、`standard` 两个 profile；`standard` 完全保持当前默认行为。
  - profile 只选择现有 ToolRegistry 中的工具和提示词片段，不复制 AgentRuntime、权限、安全 parser、MCP 或事件路由。
  - session 创建、持久化、replay、refresh 必须恢复同一个 profile；未知 profile fail-closed。
  - `permissionMode` 继续独立存在，不能被 profile 覆盖或隐式改变。
  - 集成/跨层测试：profile → runtime → tool catalog → server/SSE → session JSONL → refresh/replay，验证不同会话能力隔离、工具目录稳定和旧 `standard` 行为不变。
  - 完成记录（2026-08-24）：新增 fail-closed 的 `AgentProfileRegistry` 与内置 `standard`/`minimal`；`standard` 保持现有工具、MCP、提示词和技能行为，`minimal` 仅投影 `command`、`str_replace_editor`，不启动 MCP discovery、也不注入技能。profile 已接入 runtime、engine snapshot 和 sessions API，并随 PI session JSONL 持久化，activate、branch、refresh/replay 均恢复同一选择；旧 session 无记录时显式回退 `standard`，未知、畸形、冲突和不支持的持久化 profile 均 fail-closed。新增 registry、真实 PI SessionManager 持久化、route create/list/branch/activate、会话隔离和工具/提示词投影流程测试；typecheck、build、unit（1362/1362，serial 32 passed/1 skipped）、routes（36 文件）、frontend（502/502）、manifest 和 report 均通过，测试统计为 175 文件（73 unit、90 integration、12 system）。

- [x] **AP-02 [P0][证据链] 建立 `fact-verification` profile**
  - 首批只暴露目标文件读取、规范状态查询和必要的有限目录读取能力；禁止无关 Web、MCP、subagent、memory 写入和动态注册路径。
  - 固定完成条件：目标正文和规范状态成功载荷齐全后立即进入 `answering`；任一来源失败、缺字段或结果截断时输出“未验证”，禁止自动换工具或开放式追查。
  - 规范事实入口优先于文件搜索；调用 ledger 必须记录 canonical tool、目标、结果状态和停止原因。
  - 回归补充（2026-08-24）：任务 B 的 `list_memory(scope=user)` 成功返回“暂无记忆”后，Agent 仍发散读取 `.pi`，最终因目标不存在进入 `target_not_found_is_not_retryable_without_a_new_target`；原因是无明确文件路径的“按作用域选条目”请求未生成 `fact_verification` contract。必须支持 memory scope/entry 选择契约，并把空集合收敛为“未验证/无对象可检查”，不能继续搜索。
  - 用户展示边界：事实不足、空集合和 blocked 结果不得统一渲染为“回复失败”；内部 retry reason 不能原样作为用户错误文案，仍需保留结构化诊断和可回放原因。
  - 集成/跨层测试：成功、not-found、transport、permission、截断、长上下文、刷新恢复和多会话并发均从真实入口驱动，并验证调用数量、终态和用户可见正文。
  - 完成记录（2026-08-24）：新增独立 `fact-verification` Profile，仅投影 `file_read`、`explorer_list`、`skill_facts`、`list_memory`、`read_memory`，关闭 MCP 和技能注入，并加入专用核验提示；`standard` 和 `minimal` 行为不变。事实核验 contract 已按目标类型生成：技能目标要求正文与规范状态字段，普通文件要求完整正文，记忆作用域要求列表元数据加具体条目正文；空集合、缺字段、截断、not-found 和不支持请求均以正常 `done + 未验证` 收敛，真实权限、传输和取消失败保留错误终态。Profile session 每轮自动绑定 contract，禁止跨轮证据缓存、无关工具、重复尝试和隐式扩展到开放诊断；记忆目标按具体条目记录，source matching 不允许普通路径前缀逃逸。新增真实 HTTP 文件工具故障矩阵（成功、403、404、transport、取消、截断）、长上下文、memory entry、Profile session 并发隔离、真实 HTTP/SSE unsupported-profile、刷新/replay 回归；typecheck、build（V8 3072MB）、unit（1362/1362，serial 32 passed/1 skipped）、routes（36/36）、frontend（502/502）、manifest 和 report 均通过。当前统计为 176 文件、2422 blocks（73 unit、91 integration、12 system）。

- [x] **AP-03 [P1][架构] 抽象 ToolPresentation，先统一 native 适配器**
  - 将当前 PI 原生工具调用收口到 `ToolPresentation` 接口，先只实现 `native`，不改变执行语义。
  - presentation 层只负责模型可见工具目录和调用协议；权限、安全 parser、取消、trace、持久化仍由宿主工具流水线负责。
  - 集成/跨层测试：native presentation 与现有 live/replay/refresh、事件节点 identity、错误终态和 Electron 产物一致；禁止出现第二条隐式工具执行路径。
  - 完成记录（2026-08-24）：新增 `src/agent/tool-presentation.ts` 与唯一 `nativeToolPresentation`，`standard`、`minimal`、`fact-verification` Profile 均显式声明 `presentation=native`。runtime 的内置工具、MCP 工具和 subagent 工具全部经 native presentation 组装；Registry 只负责宿主工具注册/投影，旧 `toPITools`/转换函数仅保留为低层兼容测试出口，不作为生产装配入口。presentation 不拥有权限、安全 parser、取消、trace 或持久化，执行仍由既有 host pipeline 负责。
  - 新增 native presentation 边界测试：Profile 工具目录稳定、PI schema 保持一致、成功执行 trace、权限拒绝终态和单一适配入口；结合既有 live/replay/refresh、故障矩阵、DOM identity、dist flow 和 packaged Electron E2E 验收。typecheck 通过；routes `37/37`；build smoke `85/85`；dist flow `3/3`；packaged Electron E2E 通过（峰值 RSS `1146MB/2048MB`）。

- [ ] **AP-04 [P1][实验] PTC/Code Presentation 原型**
  - 以显式 profile 或实验开关提供 `ptc`，默认关闭；模型只看到 `run_code` 和按运行时生成的 SDK。
  - 每个子调用必须复用现有参数校验、危险命令拦截、readOnly、permissionMode、取消和 trace；父子调用具备稳定关联并可回放。
  - 必须具备 worker 隔离、代码/中间值/日志大小上限、并发安全分类、超时排空和 RSS/V8 门禁；Code Mode 不得成为安全边界的替代品。
  - 集成/跨层测试：批量只读、并发与顺序屏障、子调用失败、取消、超时、刷新/replay、packaged Electron 和内存超限均纳入门禁；未满足前不得成为默认 presentation。

- [ ] **AP-05 [P2][观察] creator 动态能力编排可行性验证**
  - 只验证 session-scoped 动态 profile/plugin 的最小原型，不修改全局 ToolRegistry，不影响其他会话，不允许直接改写 shipped preset。
  - 研究独立 worker、信任确认、资源上限、stop/dispose、持久化和撤销；优先实现“复制并编辑用户 profile”，再评估运行态插件。
  - 只有至少两个真实扩展场景需要共享安装、生命周期、权限和 UI 注册时，才进入正式 Creator 设计；否则保持观察状态。

- [x] **AP-06 [P1][生命周期] 固化 Profile 的健康、代际和切换规则**
  - profile/preset 发现必须区分 `ready`、`broken`、`unavailable`，不能静默跳过损坏目录或把展示元信息当成可执行能力。
  - 运行中的 session 固定其已组装的 profile 代际；文件变化只影响后续 session，非空 session 禁止切换，空白 session 切换失败时恢复原组装。
  - profile 选择、实际生效 profile 和每次切换必须进入可回放 session 事实；resume、fork、refresh 不能只读取创建时默认值。
  - 集成/跨层测试：损坏配置、未知 profile、并发切换、文件变更、进程重启、冷读和历史中已调用旧工具的会话均验证 fail-closed 和原子回滚。
  - 完成记录（2026-08-24）：Profile registry 新增 `ready`/`broken`/`unavailable` 健康快照、单调 generation 和历史代际解析；损坏或未发现 Profile 不进入可执行目录，未知 Profile 不静默回退。运行中的 session 固定已组装的 `{id, revision, generation}`，新的 `my-code-agent.profile.lifecycle` custom entry 记录 requested/effective、source、fingerprint、create/resume/switch/fork action 和终态；resume/fork/refresh 按 session 事实恢复，Profile 替换只影响后续 session。新增 `/api/profiles` 健康目录和空白 session 专用切换入口；非空 session fail-closed，切换装配失败由既有 transition rollback 恢复原 session/Profile。新增 `test/agent-profile-lifecycle-flow.test.mjs`，覆盖健康矩阵、历史代际、冷读持久化、目录 API、非空拒绝和并发空白切换；typecheck、AP-06 定向流程、unit 和全量 routes `37/37` 通过。

- [ ] **AP-07 [P1][状态语义] 将规划生命周期与权限策略彻底分离**
  - 保持 `PermissionMode` 只负责授权；新增独立的 plan state（`active`、`pending`、`committed`、`cancelled`），禁止用 `permissionMode=plan` 代替 Agent 规划状态。
  - 规划状态采用完整事件替换并支持 replay/refresh/fork；运行中的切换在下一个可接受步骤边界提交，明确区分排队、无变化和取消。
  - `exit_plan_mode` 必须有明确的用户批准结果；规划引导不能绕过 sandbox、permission、危险命令和工具失败终态。
  - 集成/跨层测试：命令入口、UI 切换、流式执行中切换、刷新、恢复、拒绝评审和取消 pending 选择，验证日志状态与实际执行权限不混淆。

- [ ] **AP-09 [P1][资源治理] 为 PTC 中间值和动态执行建立硬上限**
  - PTC 原型必须限制程序输入、子调用参数、中间值、日志、外层结果、并发数、执行时间和 worker heap；不能只限制最终返回文本。
  - 取消、超时、worker 崩溃和宿主退出必须排空或终止所有子调用，并留下可回放的父子结算结果；普通工具副作用不承诺回滚时必须明确记录。
  - 资源测试保留现有 RSS/V8 门禁，并报告首次超限阶段；未通过时 PTC 保持实验开关关闭。
  - 集成/跨层测试：超大 JSON、深层结构、无限输出、并发只读、exclusive 屏障、子调用失败、取消、断线、refresh/replay 和 packaged Electron。

- [ ] **AP-10 [P0][架构借鉴] 采用 dsh 的 Profile/Host-Agent 分层，避免全局硬限制**
  - 参考：`example/deepseek-harness-master/.../agent-presets/minimal`、`standard`、`code`、`cordis`；Profile 只声明当前 Agent 的能力、工具和提示词，host plane 继续拥有权限、sandbox、审批、持久化、模型路由和共享注册表。
  - `minimal`、`standard`、`fact-verification` 必须是独立能力投影；`standard` 保持当前默认开放行为，未知或损坏 Profile fail-closed，Profile 不能修改 `PermissionMode` 或绕过安全 parser。
  - plan 状态、工具呈现模式和权限策略分开建模；不能把 `permissionMode=plan` 当作完整规划生命周期，也不能为事实核验复制一套 AgentRuntime。
  - 集成/跨层测试：Profile → ToolRegistry → runtime → server/SSE → session JSONL → refresh/replay，验证会话能力隔离、未知 Profile 恢复、标准模式回归和构建产物目录一致。

- [ ] **AP-11 [P1][工具治理借鉴] 建立 Claude Code 风格的 Tool Pool、权限集和 Feature Gate**
  - 参考：`example/Claudecode/study/02-TOOL-SYSTEM.md`；统一工具注册、去重、合并和按 Agent 类型过滤，区分主 Agent、coordinator、subagent 的可见工具集合。
  - Feature Gate 只控制能力是否可见/可加载，不替代运行时权限；每个工具仍必须经过现有参数校验、危险命令、readOnly、路径授权、取消、trace 和终态处理。
  - 工具目录从实际注册表和 Profile 投影生成，禁止手工维护；模型可见但不可执行、可执行但未声明、重复工具名或权限集漂移都必须使门禁失败。
  - 集成/跨层测试：工具注册 → profile catalog → PI/native presentation → permission → execution → SSE/replay，覆盖 coordinator/subagent 隔离、MCP 合并、禁用工具和 Electron 产物。

- [x] **AP-12 [P0][指令遵从] 实施“软约束优先、硬约束兜底”的事实核验策略**
  - 普通 `standard` 任务不得套用事实核验白名单；仅在高置信度 `fact-verification` 意图下启用目标、来源、工具和证据门禁，低置信度请求保持开放探索。
  - 软层负责首选工具和停止提示；硬层只保护不可违反的来源、权限、重复调用、证据完成和终态不变量。用户明确要求扩大范围时，必须生成新的 contract revision，而不是静默放宽当前契约。
  - 记录每次任务的 `toolCalls`、`unrelatedAttempts`、`blockedAttempts`、`evidenceSatisfied`、`finalStatus`、`userExpansion` 和 token/耗时，分别统计“成功率”和“被阻止的违规尝试”，不能只看调用次数。
  - 验收基线：事实核验任务零无关成功调用、空集合正常 `unverified`、真实失败保持 `error`；普通工程任务成功率、工具选择多样性、用户显式扩展成功率不得相较 `standard` 基线下降；若下降，优先降低意图阈值或放宽软引导，不直接删除安全门禁。
  - 完成记录（2026-08-24）：新增显式 `VerificationPolicy`，普通 `standard` 的低置信度检查只注入可忽略的首选工具/停止软提示，不生成工具或来源白名单；实现、分析、审查、解释等开放任务不进入核验策略。只有显式 `fact-verification` Profile、明确 checkpoint 请求或带具体目标的“仅报告实际证据”请求才生成硬 contract；裸“继续”不再扩权，明确扩大到实现、源码或其他来源时生成新 revision 并记录 `userExpansion=true`。
  - 硬门禁收敛为 server host callback 的单一生产裁决，native adapter 只在缺少完整宿主时保留 fail-closed 兜底；来源/工具越界、重复尝试、证据已齐后的继续调用仍被阻止，权限、安全 parser 和终态门禁保持独立生效。Task lifecycle/SSE/session JSONL 现记录工具调用、无关/阻止尝试、证据完成、最终状态、用户扩展、token 和耗时。
  - 集成/跨层验收覆盖：低置信度 standard 经真实 HTTP → prompt → 多工具事件 → SSE → terminal 正常完成且工具多样性不受限；高置信度核验的来源/工具阻止、空 memory、真实 HTTP 403/404/transport/cancel/truncation、重复调用、证据终止、显式扩展 revision、refresh/replay 和持久化指标。`typecheck`、unit（`1367/1367`，serial `32 passed/1 skipped`）、routes（`38/38`）、生产 `build`、manifest/report 均在 `2048MB` V8 上限下通过；完整 `npm test` 的 10 个 Gate 全部通过，最高峰值为 routes 的 `1116MB/2048MB`，coverage 为 lines/statements `81.14%`、branches `78.39%`、functions `89.78%`。当前门禁统计为 178 文件、2440 blocks（73 unit、93 integration、12 system，61 flow）。三方真实模型策略对照仍归 AP-13，不作为 AP-12 完成条件。

- [ ] **AP-13 [P1][评测] 建立三方约束策略对照基线**
  - 对同一组任务比较本项目 `standard`、`fact-verification`、dsh 风格 Profile 和 Claude Code 风格 Tool Pool 的成功率、无关调用率、错误终态率、证据完整率、用户扩展成功率、token/耗时和内存峰值。
  - 场景至少包括：简单事实读取、空记忆、模糊指令、明确扩大范围、普通修复、多工具工程任务、权限拒绝、传输失败、刷新恢复和长上下文。
  - 测试必须使用录制事件、真实 HTTP/SSE、session replay 和 packaged Electron 流程；报告“规则拦截了什么”和“模型本来是否能完成”，避免把拦截次数误判为质量提升。
  - 只有当约束降低无关调用且不降低开放任务成功率时，才将其提升为默认 Profile；否则保留为显式模式或 shadow telemetry。

### Phase E1：NativeAgentEngine MVP

**范围：** prompt、模型流、工具调用循环、steer、follow-up、abort、事件发布和 usage 汇总。

**原则：** 第一版只覆盖已验证的核心工作流，不同时重写全部 Provider、OAuth 和模型目录；复用现有 ToolRegistry、权限系统、MCP 和子 Agent supervisor。

### Phase E2：会话与压缩独立

- 实现新建、打开、恢复和持久化会话。
- 实现上下文组装、统一 usage 接入、压缩和失败恢复。
- 使用录制事件和历史 JSONL 做兼容回放，覆盖迁移和受损会话。

### Phase E3：Provider 层逐步独立

- 定义稳定的 `ProviderDriver`，优先覆盖 DeepSeek 和 OpenAI-compatible。
- 按真实使用量迁移认证、模型目录和更多 Provider，不追求一次性全部覆盖。
- 完成迁移前，PI Provider 适配器仍作为可回退实现。

### 默认切换门槛

- 文本流、工具调用、中止、模型切换、会话恢复、压缩和子 Agent 全部通过契约测试。
- 事件顺序、usage、权限和失败语义不弱于 `PiAgentEngine`。
- 安全检查继续由宿主 Tool/权限层强制执行，模型和引擎不可绕过。
- 手工完成至少 20 次真实工程任务，并连续 4 周没有阻断级数据损坏或权限回归。
- 切换后仍保留一个发布周期的 PI 回退开关。

### 维护触发条件

- PI 稳定版本升级连续两次各消耗超过 2 个工作日，提前推进 E1。
- fork 行为补丁超过 5 个，或单次升级需要解决大面积冲突，停止增加 fork 补丁并转移到原生引擎。
- 已验证的重要产品能力被 PI 架构明确阻塞，且无法通过 E0 适配层以可控成本实现时，可将 E1 提升为阶段主线。
- 原生引擎未达到切换门槛前，不为了“拥有自己的引擎”牺牲当前可用性。
- 未触发上述条件时，E1～E3 保持未启动；一旦触发，则成为有明确里程碑的阶段主线，不与功能主线长期并行。

## 产品定位

**my-code-agent 是安全、可编排、可持续积累能力的桌面工程助手。**

- 首先服务软件工程，不转型为覆盖聊天渠道、日程和生活自动化的泛个人助手。
- 外部 agent 是可选能力提供者，不是固定的“下级”。
- 新能力必须可关闭、可审查、可测试、可回滚。
- “可持续积累”必须同时包含存储与验证，不能只增加记忆数量。

## 能力分层

```text
Tool：执行原子操作
Skill：完成某类任务的方法和流程
Agent：可被调度的角色、上下文与责任边界
Provider：本地模型或外部 agent 的执行来源
```

- 自定义 Agent 可以配置默认 Skill。
- Skill 可以声明所需 Tool，但不能扩张 Agent 的工具权限。
- Provider 不决定上下级关系，只提供能力、模型和成本选项。

## 状态说明

| 状态 | 含义 |
|---|---|
| 当前主线 | 条件明确，按顺序执行 |
| 可穿插 | 独立的小任务，不阻塞主线 |
| 值得验证 | 有价值，但先做原型或实测再决定 |
| 观察 | 记录触发条件，当前不投入 |
| 暂不计划 | 收益不足、风险过高或偏离定位 |

## 第一阶段：能力积累基础（当前主线）

Task 0～3（生成式能力目录、可信技能、spawn 环境治理、记忆数据层）均已完成，详细设计、实现和验收记录移至 [已完成任务归档.md](/E:/my-code-agent/docs/已完成任务归档.md)。第一阶段当前只保留下面的 Checkpoint A 价值复核，不继续在活跃清单重复实现历史。

## Checkpoint A：学习闭环与价值复核

Task 1～3 完成后先验证是否真的“越用越好用”，不立即进入高成本编排功能。

```text
技能或记忆被使用
  -> 记录来源与结果
  -> 用户保留、修正或禁用
  -> 比较后续命中与失败
  -> 生成技能或记忆草稿
  -> 人工审核后升级
```

**本地验证信号：**

- 技能在至少 3 个不同任务中被重复使用。
- 用户没有立即关闭或大幅重写该技能。
- 启用后产出被保留，测试或手工验收通过。
- 长期记忆至少 3 次正确命中，并减少重复说明。
- 错误记忆能够被发现、修正、追溯和回滚。

**未达到信号：** 暂缓第二阶段，继续修正技能选择、记忆作用域和用户控制，不用增加更多自动化。

## 第二阶段：长生命周期编排（Checkpoint A 通过后）

### Task 4：CostLedger 与预算护栏

**目标：** 在延长子 agent 生命周期前，建立独立成本账本、预算上限和原子中止。

**完成标准：**

- CostLedger 独立于 usage-index。
- CostLedger 只消费统一的 `ProviderUsage` 与 `PriceCatalog`，不扫描 session，也不另建 Token 计算口径。
- 支持 provider/model 输入、输出、cache 价格和计费单位。
- 主 agent 与全部子 agent usage 可以汇总。
- 未知价格明确显示“未知”，不伪造金额。
- 启动前显示成本预估；运行中显示已花费与上限。
- 到达上限后原子中止，不再启动新子任务。

**为什么做：** 长生命周期和多 agent 会放大成本，预算护栏必须先于 continuable 和会议模式。

### Task 5：Continuable subagent

**现状修正：** 只读 fan-out、worker 进程、delegate_tasks、自定义 Agent、并发限制和 usage 汇总已经完成；不再重复建设子 agent MVP。

**目标：** 让现有 one-shot 子 agent 可以继续、恢复和被主 agent 控制。

**完成标准：**

- 支持 `send`、`status`、`abort`、`resume`。
- 子任务描述、事件和结果可以持久化并冷恢复。
- 权限、预算、并发、最大轮数和最大工具调用仍由 supervisor 强制执行。
- 主会话可以查看连续事件、当前状态和最终汇总。
- 父进程退出时能可靠结束或安全保存子进程状态。

### Task 6：会议 / judge panel

**目标：** 在高风险架构决策、评审和方案比较时，允许主 agent 组织多个角色评审并收敛结果。

**依赖：** CostLedger 和 Continuable subagent 完成。

**完成标准：**

- 会议只由用户按需发起，不默认开启。
- 支持不对称模型配置：强主导模型加廉价评审模型。
- 发起前展示预算和参与者，用户确认后运行。
- 评审意见、反驳和最终结论可追溯。
- 达到预算、轮数或时间上限后自动收敛或中止。

## 可穿插的小任务

这些任务与主线依赖较少，可在大任务之间完成，但不应打断 Task 0～3。

### Q1：Permission Center 状态完善

- 补齐等待中、空态、加载失败和保存失败反馈。
- 明确当前生效的规则来源、作用域和剩余生命周期。
- 不改变现有授权语义，只改善可见性和错误恢复。

### Q2：外观用户覆盖层（原 F-2.1）

- 在现有 CSS 变量和 light theme 基础上增加用户覆盖层。
- 第一版只做背景、基础颜色和恢复默认，不建设主题市场。
- 设置必须随 data root 正确持久化，并支持预览和回滚。

## 值得验证的方向

这些功能不承诺进入正式架构，每项先通过独立原型回答关键问题。

### V1：ACP 外部 agent provider

只接一个 provider，验证启动、消息、流式事件、中止、进程树清理、权限和 usage。成功后它作为可选 Provider 进入现有编排，不把本项目降为调度外壳。

### V2：Windows 受限执行环境

独立验证普通用户权限、Electron spawn、PowerShell、Git、npm、管道输出、大工作区 ACL 传播、清理和性能。

- 它只补充写侧隔离，不能替代现有命令语义安全和路径治理。
- 必须单独实现 sandbox fail-closed：请求隔离但不可用时执行 `SANDBOX_UNAVAILABLE -> refuse`。
- 验证完成前不接入默认命令链。

### V3：Agent 代码导航工具

基于现有 tsserver 增加 definition、references、hover 等只读工具。先支持 TypeScript/JavaScript；只有实测能减少全文搜索和误读，才扩展更多语言。

### V4：运行态感知与元素定位

分阶段验证：

1. 捕获 dev server stdout/stderr，形成有界日志和错误事件。
2. 通过 Electron CDP 捕获 console error 和 network failure。
3. 用户点击问题元素，将 DOM 快照和批注作为 attachment 发送给 agent。
4. 视觉截图分析只在可用模型下启用，不作为前置条件。

观察默认采用只读；导航、点击和执行操作继续经过权限确认。

### V5：工程型后台任务

只验证测试、构建、索引等工程 job，以及完成通知、查看输出和中止。验证稳定后再讨论定时调度；不扩展成通用生活提醒系统。

### V6：增量事件投影

只为新事件增加统一 envelope、持久化规则和 projection 测试，逐步减少运行时状态与日志不一致。不一次性替换 JSONL、chatStream、SSE 和 AppEventHub。

### V7：最小插件接缝

旧 backlog 把插件系统列为“下一阶段主线”，该优先级已失效。只有出现至少两个真实扩展需要共享安装、生命周期、权限和 UI 注册机制时，才设计最小插件 API；优先复用 ToolRegistry、MCP、Skill 和 registerPane，不引入大型插件框架。

## 观察项

### O1：单窗口单项目多会话并发（④-C）

观察窗口：完成 Task 1～5 后的 4 周或 20 次真实开发任务。

只有出现至少 3 次以下情况才重新立项：用户确实需要在同一工作区维护两个独立主对话，而且多窗口、运行中补充和主 agent 委托都无法替代。

未达到条件时移入“暂不计划”，不长期悬挂。

### O2：定时调度与工作流

只有 V5 工程型后台任务被稳定使用，并出现重复的定时工程任务时再评估。第一版只能调度受权限和预算控制的工程 job。

## 暂不计划

| 功能 | 原因 |
|---|---|
| 泛个人助手和多聊天渠道 | 偏离工程工作台优势，身份、渠道和生活服务均需重新建设 |
| 固定“主人 agent / 下级编码工具”层级 | 外部 agent 应按任务能力参与，不由品牌决定层级 |
| 未经审核自动安装或永久启用技能 | 会固化错误经验，并引入提示注入和供应链风险；只允许生成待审核草稿 |
| 全面事件溯源重构 | 改动面和回归风险过高；采用 V6 渐进收敛 |
| 直接照搬 dsh Windows ACL sandbox | 当前实现是 partial，存在 ACL 残留、传播成本和管道限制；只做 V2 验证 |
| 每包 100% 覆盖率 | 单人项目维护成本过高；继续按安全和风险分配测试密度 |
| 为插件化全面拆包或引入 Cordis | 当前模块边界已可控，没有对应收益 |
| AI 安全分类器自动审批 | 不用概率模型替代现有确定性安全规则；未来最多作为提示信号 |
| 向量索引 / Meilisearch | 当前全文搜索和项目规模未证明需要额外索引基础设施 |
| 自动根据拒绝记录修改系统提示 | 容易把偶然拒绝固化成长期行为；先纳入本地学习信号观察 |
| SSH、社交、移动 VM 功能大包 | 没有明确用户需求；需要时优先以受限 Tool、MCP 或独立扩展验证 |

## 推荐执行顺序

```text
已完成（归档）：精准在线 Token -> E0-a 最小引擎契约 -> 自定义模型厂商
当前工程主线：E0-b PI 适配收尾

已完成（归档）：Task 0 能力目录 -> Task 1 可信技能 -> Task 2 spawn/env -> Task 3 记忆数据层
当前产品验证：Checkpoint A 学习闭环与价值复核
  -> Task 4 CostLedger
  -> Task 5 Continuable subagent
  -> Task 6 会议 / judge panel

可穿插：Q1 Permission Center、Q2 外观覆盖层
触发式引擎线：满足维护触发条件后，Phase E1 NativeAgentEngine MVP -> E2 会话与压缩 -> E3 Provider 层逐步独立；触发后作为阶段主线，不长期并行
独立验证：V1 ACP、V2 Windows sandbox、V3 LSP、V4 运行态感知、V5 后台任务、V6 事件投影、V7 插件接缝
```

## 来源映射

| 原文未完成项 | 本清单位置 | 处理 |
|---|---|---|
| F-2.1 外观 | Q2 | 保留，降为可穿插 |
| 插件系统 | V7 | 从主线降为有触发条件的最小接缝 |
| ④-B 后续 | Task 4～6 | 删除已完成 fan-out，只保留预算、continuable、judge |
| ④-C | O1 | 保留观察，增加明确触发和收尾标准 |
| 运行态感知 / 元素定位 | V4 | 保留，按非视觉到视觉分阶段验证 |
| Skills / 命令系统 | 归档 Task 1 | 与自定义 Agent、Tool 分层整合 |
| 会话记忆 / 后台记忆提取 | 归档 Task 3、Checkpoint A | 修正为已有 memory 基础；先显式记忆，不做自动后台提取 |
| Permission Center UX | Q1 | 保留为独立小任务 |
| CostLedger | Task 4 | 保留，并放在长生命周期编排之前 |
| 会议 / judge panel | Task 6 | 保留，依赖 CostLedger 和 continuable |
| 定时调度 / 工作流 | O2 | 依赖工程型后台任务的真实需求 |
| 沙箱 | V2 | 仅做 Windows 技术验证，不直接交付 |
| 拒绝跟踪 / AI 分类器 / 向量索引 | 暂不计划 | 当前收益不足或会削弱确定性安全边界 |
| ACP / LSP / jobs / 事件投影 | V1、V3、V5、V6 | 竞品中值得验证，不直接承诺正式架构 |
| PI 长期维护 / 自研 Agent | E0～E3 | E0 先隔离依赖；E1～E3 仅在维护或产品阻塞条件触发后启动 |

## 清单维护规则

1. 新规划必须进入本文件，并明确状态、原因、依赖和完成标准。
2. 完成后从执行区移除，只在提交记录或历史文档保留完成事实。
3. 观察项必须有时间窗口和触发条件，没有信号就关闭。
4. 值得验证的功能先写原型结论，再决定升级、继续观察或放弃。
5. `backlog.md` 和 `agent-capability.md` 后续只保留历史与能力现状，不再各自维护另一套执行顺序。
