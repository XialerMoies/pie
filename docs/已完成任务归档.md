# 已完成任务归档

- [x] **AP-14 Agent 能力选择器（2026-08-26）**：桌面端从 `/api/profiles` 展示 `standard`/`minimal` 能力选择；Profile、PermissionMode、plan state 独立显示。事实核验保持请求级 `EvidenceContract`，以不泄露诊断细节的 `evidence_state` presentation 事件贯通 SSE baseline、reconnect、live、terminal；session tab、refresh、replay、fork 不遗留临时核验状态。专项测试 51/51、build 和 typecheck 通过。

- [x] **E0-b PI 适配收尾（2026-08-25）**：主 Agent 生产装配使用 `PiAgentEngine`；PI runtime/session、context usage 和子 Agent 的 SDK 构造集中在 `src/agent-engine/pi-runtime.ts`、`src/agent-engine/pi-subagent.ts` 等适配边界；设置页和 Provider 服务只依赖项目自有 `ProviderRuntime`/`ProviderModelRegistry`，SSE 与主会话保持 `AgentEngine` 事件/快照契约。新增 E0-b 结构门禁，typecheck、routes `109/109`、子 Agent `7/7` 通过。

本文件保存已经完成、无需继续占用执行清单位置的成体系任务。新问题不得直接追加到本归档；应在 [任务清单.md](/E:/my-code-agent/docs/任务清单.md) 建立新的任务项。

## Agent 执行可靠性与证据链修复清单

完成日期：2026-08-22

共同验收口径：涉及 Agent、服务端、桌面端或事件流的任务，均通过真实入口驱动的集成/跨层流程测试；构建、Electron 和长流程保留 RSS/V8 内存门禁。简单单元测试只作为辅助，不能作为唯一验收。

- [x] **A-01 [P0] 工具结果 envelope 与失败分类**：统一 `ToolOutcome`，失败 fail-closed；跨层故障矩阵、构建产物和 routes 回归通过。
- [x] **A-02 [P0] 运行时 evidence ledger**：工具调用、成功证据、失败分类和持久化回放已关联；ledger 跨层流程与 typecheck 通过。
- [x] **A-03 [P0] 内部事件与用户展示事件收敛**：runtime → domain reducer → presentation 单链路；旧桥/replay adapter 已删除。
- [x] **A-04 [P0] 任务阶段与完成条件**：运行时维护任务阶段和终态，完成后停止重规划；任务生命周期跨层流程通过。
- [x] **A-05 [P1] 工具 canonical identity/别名解析**：registry 统一规范名和重复判断；canonical identity、MCP、回放及全量回归通过。
- [x] **A-06 [P1] 刷新后的技能事实源**：snapshot/revision 统一 trust、enabled、parse、fingerprint 与正文注入；刷新/恢复/设置路由流程通过。
- [x] **A-07 [P1] 按错误类别重试**：重试由错误类别和请求指纹驱动，重复失败进入明确终态；故障矩阵跨层流程通过。
- [x] **A-08 [P1] 跨层确定性事件脚本**：覆盖 live、SSE、reconnect、replay、Ctrl+R、dist/Electron，逐事件断言通过。
- [x] **A-08a [P1] 事件流测试退化门禁**：要求中间轨迹、节点 identity、DOM mutation 和恢复一致性；流程及 dist 门禁通过。
- [x] **A-09 [P2] 死路径和状态污染清理**：旧事件桥、生产测试钩子、重复 context 包装已移除；结构门禁和 routes 隔离回归通过。
- [x] **A-10 [P2] 任务/证据/事件诊断关联**：CorrelationLedger 贯通 live/replay/session/diagnostics，诊断脱敏；跨层流程和 Electron E2E 通过。
- [x] **A-11 [P1] 构建峰值与内存门禁**：构建阶段解耦并串行化；build、dist、Electron E2E 及峰值门禁通过。
- [x] **A-12 [P0] 工具失败后的运行时终态门禁**：失败、拒绝、取消和迟到事件均进入明确终态；流程测试通过。
- [x] **A-13 [P0] Thought/trace 与用户展示隔离**：内部诊断事件与普通正文、SSE、回放消息隔离；跨层和 Electron 测试通过。错误终态统一追加到事件节点流末尾，以“错误类型：错误原因”呈现；旧错误卡片、详情和下一步操作入口已删除。最新前端 `503/503`、构建和 packaged Electron E2E 通过，E2E 峰值 RSS `1134MB/2048MB`。
- [x] **A-14 [P1] TypeScript diagnostics 去重与超时降级**：latest-wins、取消和结构化超时/失败状态完成；并发流程通过。
- [x] **A-15 [P1] 会话列表增量索引**：增量扫描、摘要缓存和部分结果状态完成；并发读取流程通过。
- [x] **A-16 [P1] session-independent API 并发隔离**：独立 API 不再等待 session ready；真实并发流程通过。
- [x] **A-17 [P1] 规范事实查询入口优先级**：文件目标的事实核验契约、规范状态查询、证据硬停止和错误终态已完成；任务 A 有成功证据，任务 B/C 暴露了无明确目标的 memory scope 核验和空集合终态缺口，后续修复已转入活跃清单 AP-02。
- [x] **A-18 [P1] 长工具期间桌面端并发流程**：packaged Electron、SSE、diagnostics、侧栏/设置和内存门禁流程通过。

## P0 发布硬化与基础设施（截至 2026-08-23）

- [x] 发布门禁已覆盖 typecheck、全量测试、build、smoke 和 packaged Electron E2E；JSON/JSONL 持久化恢复、结构化日志与脱敏诊断、Provider 兼容矩阵和 SSE 生命周期均已纳入测试。
- [x] PI fork 锁定 `@xiamol/pi-coding-agent@0.84.2-xiamol.0`，保持薄 fork；项目自有 `AgentEngine`/`PiAgentEngineAdapter` 边界已建立。
- [x] 精准在线 token usage、自定义模型厂商边界及 Windows Git Bash/Terminal 体验已完成。
- [x] 全量 routes 启动 readiness 稳定化（2026-08-24）：`multi-instance-launch` 测试不再把 `SERVER_PORT` 日志当作可用条件；改为等待真实 `/api/bootstrap` 成功响应，保留有界超时、子进程退出、端口冲突和资源耗尽分类。单叶子与全量 routes `37/37` 通过。

真实 Provider、真实用户数据目录、升级迁移、更多平台、宿主休眠/断电与长期运行仍属于发布环境验收，不因本节归档而视为完成。

## 测试质量体系 T-01～T-07（2026-08-23）

- [x] **T-01 Replay-first**：统一 replay/record/refresh fixture，贯通 AgentEngine、SSE、JSONL、前端与刷新恢复。
- [x] **T-02 状态机不变量**：统一事件 seq、工具生命周期、block identity、终态不可变、证据完整性及 live/replay/refresh 收敛检查。
- [x] **T-03 Packaged Electron**：真实 Windows packaged 产物覆盖长工具、并发 API、断线重连、取消、刷新、会话切换和 DOM identity。
- [x] **T-04 失败 artifact**：统一事件、网络、DOM/ARIA、session、console、截图、进程峰值及 hash/脱敏门禁，并支持单命令重放。
- [x] **T-05 Gate DAG 与资源治理**：测试依赖、RSS/V8 上限、skip/live/platform 例外和 coverage collector 均进入机械门禁。
- [x] **T-06 Agent 行为评测**：5 个基础 replay 加 4 个脱敏工程任务录制，覆盖模糊指令、用户纠偏、64 消息/约 48K 字符长上下文和多工具重构；评测最终正确性、工具/路径范围、最新指令归属、工具多样性、证据、终态、泄漏及 inspect → edit → verify 顺序。九类确定性故障注入均能触发退化门禁；该录制集不替代真实 Provider 评测。
- [x] **T-07 发布可靠性流程**：覆盖 2,000 事件长 SSE、真实 HTTP 断线恢复、server crash/restart、大 session、损坏 JSONL 和原子写入失败。

当前分层、flow 和测试文件数量只以 [自动生成统计](/E:/my-code-agent/docs/generated/test-stats.md) 为准；当前 Agent 场景结果只以 [自动生成行为报告](/E:/my-code-agent/docs/generated/agent-behavior-report.md) 为准。

## 能力积累基础 Task 0～3（2026-08-19～2026-08-20）

- [x] **Task 0 生成式能力目录**：由代码生成工具、spawn、事件、API、权限模式和技能 catalog，CI 检查同步。
- [x] **Task 1 可信技能 MVP**：实现用户级/工作区级 `SKILL.md` 扫描、解析、信任、启停、指纹变更失信及按需正文注入；技能不能扩张工具或绕过权限。
- [x] **Task 2 spawn 与环境治理**：用户命令、MCP、tsserver、内部 server 和 Electron helper 使用独立环境继承策略，spawn owner 与凭据脱敏受目录门禁约束。
- [x] **Task 3 记忆数据层**：用户级、工作区级和会话级作用域明确；长期记忆支持索引、启停、删除、来源追踪、按需读取及旧目录安全迁移。

Task 1～3 的真实价值继续由活跃清单中的 Checkpoint A 验收，不因实现归档而自动判定产品价值闭环完成。

## 维护性审查清单（2026-08-21～2026-08-23）

> 来源：服务端、Agent、Electron 桌面端和测试/构建流程的只读审查。按建议顺序排列；`确认问题` 表示已有直接证据，`兼容债务` 表示当前仍有用途但缺少收敛期限，`有意分支` 表示不是死代码但需要明确唯一所有权。

### 测试基线来源

测试文件、层级、flow 和声明块数量由 `scripts/test-manifest.mjs` 与 `scripts/test-report.mjs` 生成，归档不再保存会过期的手工数字。查看 [当前自动生成统计](/E:/my-code-agent/docs/generated/test-stats.md)；实际 pass/fail/skip 仍以对应测试运行的 TAP 输出为准。

- [x] **M-01 [P1][确认问题] 接入遗漏的 10 个活测试文件**
  - 范围：`package.json` 的 `test:unit`/`test:routes`/`test:frontend` 手工列表、CI 的 `npm test`。
  - 证据：`test/agent-engine-semantic-contract.test.mjs` 等 10 个文件未被标准脚本引用；单独执行结果为 41/41 通过。
  - 完成标准：测试入口改为自动发现或由单一 manifest 生成；CI、`npm test`、`test:parallel` 覆盖一致；新增测试无需再手工改多处列表。
  - 完成（2026-08-22）：新增 `scripts/test-manifest.mjs` 自动发现并分类全部 162 个测试文件（含串行 `multi-instance-e2e.mjs`）；unit/routes/frontend/build 入口统一读取 manifest，移除 package/routes/build 中重复手工列表；`test:manifest`、unit、routes、frontend、build 门禁均通过。

- [x] **M-02 [P1][兼容债务] 收敛服务端旧/新事件桥**
  - 范围：`src/server/agent-event-router.ts` 的旧 PI/session 桥与 `attachEngineEvents`。
  - 证据：旧桥曾从 `src/server/server.ts` 导出并被旧测试调用，生产启动使用新桥；两套都处理 lifecycle、usage、trace、block 持久化。
  - 完成标准：只保留一个生产事件语义；旧 fixture 改走新桥或明确隔离为 replay adapter；删除旧桥导出和重复行为测试；节点流、结束态、持久化回归全量通过。
  - 完成（2026-08-22）：旧会话不属于支持范围，已删除 `src/server/replay-event-adapter.ts`、旧桥专用测试和 `attachSessionEvents`/`attachReplayEvents` 引用；生产只保留 `attachEngineEvents`。AppEventHub、SSE、文件监听和生产事件流测试仍保留。

- [x] **M-03 [P2][确认问题] 统一 ServerContext 的 grouped/flat 访问契约**
  - 范围：`src/server/server-context.ts`、`src/server/server-bootstrap.ts`、`src/server/routes/types.ts` 及 settings 路由。
  - 证据：启动时创建五组 grouped context，又通过 getter 暴露 flat 兼容字段；`resolveEngine` 仍保留 grouped/flat fallback；路由混用两种访问方式。
  - 完成标准：grouped context 成为唯一路由契约；兼容 getter 限制在迁移适配层；删除 fallback 后 `server-context`、路由结构和启动测试通过。
  - 完成（2026-08-22）：生产 `ServerContext` 仅暴露 `groups`，删除全部 flat getter 与 `resolveEngine` fallback；服务端路由、HTTP 安全层、事件路由和共享授权入口统一按 core/security/storage/providers/infra 访问。新增 grouped-only 结构、启动装配到授权边界的集成测试；定向测试 `23/23` 通过，routes 隔离回归通过。

- [x] **M-04 [P2][兼容债务] 合并 Agent 初始化装配入口**
  - 范围：`src/agent/index.ts` 的 `initEngine`、`initAgentHost`、`initAgent`。
  - 证据：`initEngine` 与 `initAgentHost` 都重复执行 `AgentRuntime.create()` + `PiAgentEngineAdapter`；`initAgent` 当前无生产调用点。
  - 完成标准：增加一个内部 host factory；CLI、桌面服务端和测试使用同一装配逻辑；`initAgent` 明确删除或写入有期限的兼容说明与门禁。
  - 完成（2026-08-22）：新增唯一 `createAgentHost`，`initEngine` 与 `initAgentHost` 共用 `AgentRuntime.create` 和 `PiAgentEngineAdapter` 装配；删除无生产调用的 `initAgent`。新增结构门禁验证两条入口无重复初始化，AgentEngine/PI adapter/subagent wiring 定向测试 `23/23` 通过。

- [x] **M-05 [P2][有意分支] 收敛命令安全 parser 路径并增加 shadow CI 门禁**
  - 范围：legacy parser、异步 Tree-sitter、显式 Tree-sitter、shadow/shadow-only、AST/文本 dangerous checks。
  - 证据：`security-parser.ts` 同时暴露同步/异步/Tree-sitter 入口；执行路径使用异步解析，shadow 差异默认只记录日志。
  - 完成标准：统一到一个 SecurityParseResult 管线；legacy 只作为明确的 fail-closed fallback；CI 固定运行两个 shell 方言和 shadow parity 矩阵，差异必须失败而不是只打日志。
  - 完成（2026-08-23）：生产 POSIX 入口统一使用 Tree-sitter `SecurityParseResult`；Tree-sitter 不可用时返回 `too-complex` 并进入确认/拒绝，不静默降级 legacy。cmd 分支保留明确命名的 `parseCommandForSecurityLegacyFallback`；移除 `shadow-only` 对生产结果的旧路径切换。新增固定 POSIX/cmd parity flow 和 verdict shadow 门禁，`test:security-shadow` 已接入 Windows governance CI，差异直接失败；typecheck、manifest、命令安全回归 `392/392`，shadow flow `4/4` 通过（2GB V8 上限）。

- [x] **M-06 [P2][确认问题] 去重 Agent 工具上下文类型与转发层**
  - 范围：`RuntimeConfig`、`RuntimeToolExtraContext`、`ToolExecutionExtraContext`、`tools/index.ts` 的 `ExtraCtx`。
  - 证据：权限、确认、记忆、subagent 等字段在多个类型和包装函数中重复声明/转发。
  - 完成标准：定义一个共享的宿主工具上下文类型；`buildToolContextExtra`、`getCustomTools`、`toPITools` 只保留一条类型安全链；新增字段只需修改一个契约和对应测试。
  - 完成（2026-08-22）：新增共享 `ToolHostContext`；`RuntimeConfig` 扩展该宿主契约，`ToolExecutionExtraContext` 统一为 `Partial<ToolHostContext>`，单次执行态仍由 `ToolContext` 独立承载。删除 `RuntimeToolExtraContext` 和字段级重复声明；新增结构门禁及 RuntimeConfig → registry → PI execute 跨层转发测试，相关测试 `400/400`、typecheck 通过。

- [x] **M-07 [P2][有意分支] 明确 Electron server binding 生命周期唯一所有者**
  - 范围：`electron-main.ts`、`window-manager.ts`、`server-binding.ts` 的 external/owned/initial/window recovery 路径。
  - 证据：主进程有 `startPiServer`/`stopPiServer`，WindowManager 又为窗口创建、停止和恢复 owned binding。
  - 完成标准：明确主进程与 WindowManager 的生命周期边界；启动、切换、异常重启、退出各只存在一个状态机；补充多窗口/外部 server/重启矩阵测试。
  - 完成（2026-08-23）：WindowManager 新增 `adoptInitialServerBinding`、`startInitialServer`，统一接管初始 binding 的启动与退出；Electron 主进程只请求 WindowManager，不再直接调用 binding `start/stop`。`disposeAll` 能区分“尚未进入窗口上下文的初始 binding”和“已转为窗口 binding”的同一对象，保证退出只 stop 一次；已有 external/owned、切换、异常重启、关闭恢复路径统一经过 WindowManager。新增初始启动前退出和初始 binding 转窗口后单次停止测试；Electron 生命周期定向测试 `84/84`、typecheck 通过（2GB V8 上限）。

- [x] **M-08 [P3][确认冗余] 清理写入但不参与决策的状态和生产测试钩子**
  - 范围：`electron-main.ts` 的 `mainWindow`，以及 `tools/index.ts` 的 `_getMcpCacheLen`/`_setMcpCache` 等测试专用导出。
  - 证据：`mainWindow` 仅声明、赋值和关闭清空，实际窗口状态由 WindowManager 管理；MCP cache 测试钩子位于生产模块。
  - 完成标准：删除无业务用途的状态；测试钩子移入 test-only helper 或明确 `@internal` 边界；结构测试防止回流。
  - 完成（2026-08-23）：删除 Electron 主进程的 `mainWindow` 镜像状态及关闭清理监听，窗口状态统一由 WindowManager 持有；确认生产 `tools/index.ts` 已无 `_getMcpCacheLen`/`_setMcpCache` 测试导出，MCP 缓存仅保留真实 discovery 生命周期。结构门禁新增 `electron-main.ts` 不得出现 `mainWindow`，并继续防止 MCP 测试钩子回流。

- [x] **M-09 [P2][确认问题] 扩大前端 TypeScript 类型检查范围**
  - 范围：`src/frontend/tsconfig.frontend.json` 与 `scripts/compile-frontend-ts.mjs`。
  - 证据：前端有 64 个 `.ts` 源文件，tsconfig 直接覆盖的业务源文件只有 8 个 service 文件；`noUnusedLocals` 和 `noUnusedParameters` 均关闭。
  - 完成标准：所有前端源模块进入 typecheck；对历史全局脚本引入的确切例外单独声明；启用可行的 unused 检查并逐项清理或标注必要例外。
  - 完成（2026-08-23）：`src/frontend/tsconfig.frontend.json` 覆盖全部 `70` 个前端 `.ts` 源文件和 `dashboard.d.ts`，并排除生成目录；新增 `check-frontend-type-coverage.mjs` 结构门禁。按现有架构明确分层：legacy 全局脚本全量做 TypeScript 语法/覆盖检查，`services/**/*.ts`、`service` 迁移完成后纳入 `tsconfig.frontend.strict.json` 的严格 typecheck；`typecheck:frontend` 先跑覆盖门禁，再跑严格模块和全量语法门禁。该例外有配置和测试记录，后续模块迁移后必须收紧，不能永久兼容。

- [x] **M-10 [P3][确认问题] 让测试与文档统计自动生成**
  - 范围：`AGENTS.md` 的测试数量、`docs/backlog.md` 中历史测试数字、package scripts。
  - 证据：2026-08-22 实跑为 unit `1291`（`1290` pass、`1` skipped）、routes `392/392`、frontend `494/494`；routes 现由隔离 runner 逐文件执行。`docs/backlog.md` 和历史验收记录仍存在固定旧数字，测试入口也仍由多处手工列表维护。
  - 完成标准：CI 输出并校验实际 suite/test 数；文档只记录生成时间或使用自动生成摘要，禁止手工维护易过期数字。
  - 完成（2026-08-23）：新增 `scripts/test-report.mjs` 和 `npm run test:report`，可按需生成本地 `docs/generated/test-stats.json` 与 `test-stats.md`，输出 suite/layer/flow/默认门禁和声明测试块统计；`npm test` 先执行 manifest 与报告校验；新增结构测试验证报告来源和口径。AGENTS.md 只保留命令及生成报告入口，不再作为当前测试数量事实源；运行时 pass/fail/skip 仍必须来自对应 TAP 输出，不以发现文件数冒充执行结果。

- [x] **M-11 [P1][测试质量] 建立测试层级和关键流程可信度门禁**
  - 范围：全部 `test/**/*.test.mjs`、`test:unit`/`test:routes`/`test:frontend`、构建产物和 Electron E2E 入口。
  - 现象：当前执行总量为 `2177`（含 1 skipped），但严格跨层流程测试约 `34` 个，系统/E2E 仅 `21` 个；大量测试只检查单个函数、最终 DOM 或最终字符串，无法发现实时事件流的中间时序错误。
  - 目标：为每个测试声明唯一层级（unit/integration/system）和可选的 `flow` 标签；把 Agent → server → SSE → frontend/persistence 的确定性事件脚本、真实 `dist`/Electron、断线重连和 Ctrl+R 回放纳入默认质量门禁。
  - 完成标准：测试 manifest 自动生成并校验分层统计；关键流程必须逐事件断言状态轨迹、block identity、seq、DOM mutation、持久化和恢复一致性；只测最终结果的测试不能作为事件流缺陷的唯一验收；`npm test` 与发布门禁覆盖同一 manifest。
  - 完成（2026-08-22）：manifest 为每个测试文件固化 `suite/layer/flow/default`；新增 manifest 结构门禁和串行/内存受控 runner。unit `1338+32`（1 skipped）、routes 28 文件、frontend `497/497`、build smoke `85/85` + dist flow `3/3` 全部通过；关键事件流已在源码、routes/SSE、前端 DOM、持久化回放和 dist 构建产物层逐事件验证。live provider 测试保持显式 opt-in，不混入默认门禁。

- [x] **M-12 [P1][兼容债务] A-01 结构化工具结果兼容层退出**
  - 现状：生产路径已不再接受 `string` 结果、缺失 `outcome` 或非法 envelope；旧结果兼容归一化、兼容字段和迁移遥测已删除。
  - 目标：生产 ToolRegistry/PI adapter 只接受显式结构化结果；未声明或不符合契约的工具进入明确的 `tool_result_contract_required` 失败终态；legacy 归一化仅保留在明确隔离的 replay/test adapter（如仍需要）。
  - 集成/跨层测试目标：从内置工具、MCP、第三方注册工具、ToolRegistry → PI adapter → runtime → server → session/diagnostics → packaged Electron 真实入口驱动；逐事件验证成功/失败、日志、证据、回放和终态，确认 live 链路只出现结构化结果。
  - 删除条件：结构化结果字段成为强制类型和注册门禁；真实生产来源及发布构建流程持续通过严格契约；替代错误协议、拒绝/恢复行为和迁移测试均通过。上述条件已满足，兼容分支、字段、计数器和日志已删除。
  - 完成（2026-08-23）：`AgentTool.execute` 与 MCP adapter 统一返回显式 `AgentToolResult`/`outcome`；ToolRegistry/PI adapter 对未声明、string、缺失 outcome、malformed envelope 统一进入 `tool_result_contract_required` 失败终态，不生成成功 evidence。内置工具的错误字符串路径已迁移为 `structuredToolError`；live observability、diagnostics 和 evidence ledger 删除 `legacy`/`missingOutcome`/`invalidOutcome` 字段与迁移计数器。新增 runtime → registry → PI → trace 的跨层拒绝矩阵，覆盖上述结果类型、MCP 和结构化失败；定向结构化结果回归 `54/54`、边界 flow `3/3`、typecheck 通过（2GB V8 上限）。

- [x] **M-13 [P1][维护性] 拆分 Agent 类型契约与 Runtime 宿主装配边界**
  - 范围：`src/agent/types.ts`、`src/agent/runtime.ts`、工具注册/PI adapter、RuntimeConfig 与工具上下文构造。
  - 现象：类型文件同时承担结果归一化、授权包装、PI adapter 和 Registry；runtime 同时承担宿主配置转发与 session 生命周期，修改任一边界都需要阅读大文件并容易产生重复实现。
  - 完成（2026-08-23）：新增 `tool-outcomes.ts`（结构化结果、失败分类、证据/执行契约）、`tool-registry.ts`（授权、PI adapter、Registry）和 `runtime-config.ts`（RuntimeConfig、宿主上下文构造）；生产工具注册入口已切换到新 Registry，`runtime.ts` 不再持有配置/上下文构造实现。`types.ts` 已降至 465 行，仅保留核心契约类型与显式兼容 re-export；旧授权/adapter/Registry 实现已删除。共享授权标记保证迁移期间重复导入不会重复授权。
  - 验证：新增 RuntimeConfig → Registry → PI execute → trace/outcome 跨层流程测试，覆盖成功、结构化失败、host capability 转发和 trace 终态；typecheck、unit `1350/1350`（serial `32` 通过、`1` skipped）、routes `29` 个文件和 frontend `497/497` 全部通过；默认 `npm test` 退出码 `0`；`npm run build` 在 `NODE_OPTIONS=--max-old-space-size=2048` 下成功生成 server、Electron preload 和 Vite dist 产物。

## 归档说明

- A-01 的结构化结果兼容层退出已在 M-12 完成；生产链路不再接受字符串、缺失 `outcome` 或非法 envelope。

## Agent Profile 与工具呈现（2026-08-24）

- [x] **AP-01 [P0][指令遵从] AgentProfile 注册表与 session 能力投影**
  - 新增 fail-closed 的 `AgentProfileRegistry` 与内置 `standard`/`minimal`；Profile 只投影工具、提示词、MCP 和技能依赖，不复制 AgentRuntime、权限、安全 parser 或事件路由。
  - Profile 已接入 runtime、engine snapshot、sessions API 和 PI session JSONL；create、activate、branch、refresh/replay 恢复同一选择，未知、畸形、冲突和不支持的持久化 Profile 均 fail-closed。
  - Registry、真实 PI SessionManager 持久化、route、会话隔离和工具/提示词投影流程已通过 typecheck、build、unit、routes、frontend、manifest 和 report 验收。

- [x] **AP-02 [P0][证据链] 请求级 EvidenceContract**
  - 事实核验不再作为可持久化或可切换的 Agent Profile；`standard`/`minimal` 保持原能力面，由宿主按高置信度请求临时绑定 `fact_verification` contract。`file_read`、`skill_facts`、`list_memory`、`read_memory` 的结构化证据字段、来源白名单、顺序和缺失字段终态继续由 host contract 约束。
  - 空集合、缺字段、截断、not-found 和不支持请求以 `done + 未验证` 收敛；真实权限、传输和取消失败保留错误终态。禁止跨轮证据缓存、无关工具、重复尝试和隐式扩大调查范围。
  - 真实 HTTP 文件故障矩阵、memory entry、长上下文、多 session、HTTP/SSE、刷新/replay、typecheck、build、unit、routes、frontend、manifest 和 report 均通过。

- [x] **AP-03 [P1][架构] Native ToolPresentation 适配器**
  - 新增唯一 `nativeToolPresentation`；内置、MCP 和 subagent 工具均经该入口组装，presentation 只负责模型可见目录和调用协议，权限、安全 parser、取消、trace、持久化继续由宿主流水线负责。
  - Profile 目录、PI schema、成功/拒绝终态、live/replay/refresh、DOM identity、dist flow 和 packaged Electron E2E 均通过，生产不存在第二条隐式工具执行路径。

- [x] **AP-06 [P1][生命周期] Profile 健康、代际和切换规则**
  - Registry 明确 `ready`、`broken`、`unavailable` 健康状态、单调 generation 和历史代际；损坏或未知 Profile 不进入可执行目录，也不静默回退。
  - session 固定已组装的 `{id, revision, generation}`，生命周期事实进入 JSONL；resume、fork、refresh 按 session 事实恢复，非空 session 禁止切换，空白 session 切换失败原子回滚。
  - 健康矩阵、历史代际、冷读持久化、目录 API、非空拒绝和并发空白切换流程已通过 typecheck、unit 和 routes 验收。

- [x] **AP-08 [P1][契约门禁] 按 Profile 生成工具与提示词目录**
  - 新增由实际 `ToolRegistry.project()`、`ToolPresentation.present()` 和 prompt section registry 生成的稳定 catalog；覆盖工具顺序、执行状态、schema、权限/风险/operations、禁用工具、presentation、MCP/skills 依赖和提示词 section 指纹。
  - `profiles:generate`/`profiles:check` 管理确定性文档生成物，`/api/profiles` 返回相同目录，server 启动前执行 ready preflight；`broken`/`unavailable` 保留状态但不生成工具或回退到 `standard`。
  - 门禁覆盖三套内置 Profile、双向声明/执行漂移、API、dist 产物和 packaged Electron；typecheck、定向测试 `10/10`、dist `4/4`、packaged Electron E2E 均通过，桌面流程峰值 RSS `1138MB/2048MB`。

- [x] **AP-07 [P1][状态语义] 将规划生命周期与权限策略彻底分离**
  - 新增 session-scoped 的 `PlanStateSnapshot`（`active`、`pending`、`committed`、`cancelled`），以完整 replacement 事实写入 session JSONL，并接入 Engine snapshot、`plan.changed`、SSE、replay、refresh、fork 和 sessions API。运行中的目标切换排队到 `agent_end` 安全边界；无变化为 no-op，用户取消保留明确 `cancelled` 终态。
  - `PermissionMode` 继续只负责授权，原 `plan` 枚举仅表示“逐次确认”；规划状态独立阻止写工具，即使 `permissionMode=yes` 也不能绕过，动态只读 command 仍可探索。`enter_plan_mode`/`exit_plan_mode` 通过宿主控制器进入；退出必须经过 `plan_confirm` 和匹配的用户批准，拒绝或中断恢复为 `active`。
  - 前端计划模式改为服务端状态 API，不再使用提示词前缀伪造状态；普通展示事件通过 presentation boundary 输出 `plan_state`，不会混入 trace、Thinking 或权限语义。旧事件订阅保持单一路径，安全边界复用既有 session event subscription。
  - 新增 `test/plan-state-flow.test.mjs` 和跨层回归：真实 SessionManager 冷恢复、runtime 事件订阅与单订阅计数、流式 pending/边界提交/no-op/cancel、plan gate 与只读探索、HTTP、SSE/replay、批准/拒绝、sessions create/branch/activate 的 plan state 保留、前端真实状态 API。
  - 验证（2026-08-24）：`npm run typecheck`、unit `1367/1367`、routes `39/39`、frontend `503/503`、`npm run build`、build smoke `85/85`、dist 跨层 `4/4`、`profiles:check` 和 capability catalog 同步均通过；构建 frontend 峰值 RSS `1183MB/3584MB`，dist flow 峰值 `270MB/2048MB`。新增测试不是仅单元断言，覆盖 runtime → engine → server/SSE → session JSONL → frontend/dist 的组合流程。

- [x] **AP-10 [P0][架构] Profile/Host-Agent 分层**
  - `AgentProfile` 只声明工具、提示词、呈现模式、Feature Gate 和 MCP/skills 依赖；host plane 继续唯一拥有权限、安全 parser、plan state、sandbox/path、审批、模型路由、持久化、trace 和终态。`standard` 保持开放默认，`minimal` 提供收缩能力投影；事实核验通过请求级 EvidenceContract 叠加，不改变 Profile。未知、损坏、未知/重复 Feature Gate 及 MCP gate 漂移均 fail-closed。
  - Profile fingerprint、生成 catalog、session selection/lifecycle、HTTP API、refresh/replay 和构建产物使用同一声明；没有复制第二套 AgentRuntime，也没有用 `PermissionMode` 表示规划或能力可见性。

- [x] **AP-11 [P1][工具治理] Tool Pool、Agent audience 与 Feature Gate**
  - 新增单一 `ToolPool`，统一 native/MCP 身份、严格去重和顺序稳定合并；工具按 `main`、`coordinator`、`subagent` audience 投影，原只读子 Agent 名单由 ToolPool 唯一维护，embedded subagent 也从同一池装配并保留最终 `isReadOnly` 防御。
  - Feature Gate 只决定工具可见/是否触发 MCP discovery，不产生授权；可见工具仍完整经过 native presentation、参数/结果契约、plan gate、permission、安全 parser、路径授权、trace/outcome 与 terminal。Profile catalog 改由 ToolPool 生成并公开 `featureGates`、每个工具的 `feature`、`source` 和 `audiences`；未声明工具只进入 `disabledTools`，不可执行工具、重复身份和权限集漂移使 preflight 失败。
  - 验证（2026-08-24）：新增 `test/tool-pool-profile-flow.test.mjs`，覆盖 native/MCP 重名、三类 audience、web/delegation/MCP gate、权限拒绝、presentation → SSE → reconnect replay 终态和标准 Profile 回归；subagent factory 增加 `command` 越权输入拒绝。定向流程 `32/32`、typecheck、unit、routes `40/40` 文件、frontend `503/503`、build smoke `85/85`、dist `4/4` 均通过；真实 packaged Electron E2E 验证 `/api/profiles` catalog，峰值 RSS `1130MB/2048MB`。

- [x] **AP-12 [P0][指令遵从] 软约束优先、硬约束兜底的事实核验策略**
  - 新增显式 `VerificationPolicy`：普通 `standard` 的低置信度检查只给软提示，开放工程任务不进入事实核验白名单；只有明确 checkpoint、只报告实际证据或具体目标的请求才生成硬 contract。事实核验不再通过 Profile 触发。
  - 硬门禁由 server host callback 单点裁决，只保护来源、权限、重复调用、证据完成和终态不变量；用户明确扩大范围时生成带 `userExpansion=true` 的新 revision，不静默放宽原契约。
  - Task lifecycle、SSE 和 session JSONL 记录调用、阻止尝试、证据状态、最终状态、扩展、token 和耗时；真实 HTTP/SSE、故障矩阵、refresh/replay、typecheck、build 和完整 10 Gate 回归均通过。三方真实模型策略对照继续由活跃任务 AP-13 跟踪。
